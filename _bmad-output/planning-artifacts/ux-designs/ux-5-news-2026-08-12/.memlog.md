---
topic: 5 News mad-libs page (Epic 4)
updated: 2026-08-13T23:10
---

- (assumption) Sources scanned: epics.md (Epic 4, 8 stories, FR-1..FR-19, NFR-1/NFR-4) and ARCHITECTURE-SPINE.md (Zone/Period/Language addressing, JSON schema at data/briefings/<lang>/<zone>/<period>.json). 4 open [ASSUMPTION] tags to resolve: FCP 1s p95 target, ~240-320 char/summary length, language-control placement outside the mad-libs sentence, WCAG 2.1 AA target.
- (decision) Working mode: Fast path, full autonomy granted by user -- no further clarifying questions; author both spines directly, using [ASSUMPTION] tags only where a genuinely open design choice was made, and move straight to Finalize without additional discovery turns.
- (decision) Resolved all 4 PRD [ASSUMPTION] tags directly, no further user check-in requested (full autonomy granted): (1) FCP 1s p95 confirmed as-is -- already consistent with the static-build architecture, no contrary signal. (2) Summary length target set to ~260 characters (midpoint of the PRD's 240-320 suggested range) -- 'readable in one breath'. (3) Output Language control placed top-right of the page header, outside and above the mad-libs sentence -- keeps the sentence at exactly two blanks (Period, Zone) per FR-2/FR-3's own framing. (4) WCAG 2.1 AA confirmed as the accessibility floor.
- (decision) Form factor: mobile-first responsive web, single-surface (no native app shell beyond Epic 5's PWA installability). No auth, no personalization, no account -- every visitor sees the same generated set. Visual direction: editorial/measurement register, not a colorful consumer-app register -- the product's own architecture repeatedly frames ranking as 'a measurement, not an opinion' (rank.py docstring, PRD Glossary); the UI should read like a data-backed wire report, not a social feed. Typography-led, restrained color, numbers treated as first-class content (Consensus Score is not a footnote, it's the credibility mechanism).
- (decision) Both spines drafted in full: DESIGN.md 'Wire & Ledger' (editorial-restraint register, serif headlines + grotesque body + monospace numerals reserved exclusively for Consensus Score/Discarded Volume, forest-green single accent for interactivity, brick-red single accent reserved only for the Continent-fallback notice). EXPERIENCE.md: single-page IA addressed by (Language, Zone, Period), 4 stacked regions (header, mad-libs sentence, item list, Discarded Volume + End Screen), 3 named journeys (Amara/first-arrival, Kwame/expatriate-fallback, Priya/skeptical-verification). All 4 PRD [ASSUMPTION] tags resolved per the earlier decision entry; 2 new [ASSUMPTION] tags introduced for genuinely new choices this spine had to make (exact language-control placement rationale; no cookie/localStorage persistence for language choice, URL-only).
- (event) 2 key-screen mocks rendered and promoted to mockups/: briefing-world-day.html (canonical entry, World/day/FR, 4 items) and briefing-fallback.html (Continent-fallback notice + Consensus chip expanded state). Both linked inline from EXPERIENCE.md's Information Architecture section. Reviewer Gate skipped per the standing full-autonomy instruction -- no clarifying/validation question asked.
- (event) spines finalized
- (override) Post-finalization correction, surfaced during Story 4.1 spec prep: DESIGN.md/EXPERIENCE.md and both mockups originally implied a separate bolded headline field per item (h2 + distinct summary paragraph), but pipeline/domain/__init__.py's BriefingRecord schema has no such field -- only a single string per Cluster (Summary's own domain definition: 'a trailer, not a substitute'). User decided: no separate title -- the Summary is the item's entire display copy. Corrected both spine files' Brand & Style/Typography/Information Architecture/State Patterns/Accessibility Floor sections and both HTML mockups (merged h2+p.summary into one serif paragraph) to match the real data contract rather than inventing a field.
- (override) Story 6.1 reverses the 2026-08-12 (override) entry below that removed the per-item headline. That removal was explicitly a data-contract concession, not a product judgement -- its own wording was 'to match the real data contract rather than inventing a field.' Story 6.1 makes the field exist: BriefingRecord gains a per-Cluster 'headline' (schema_version 1 -> 2), generated by the same summarize batch call as the Summary via a json_schema output_config returning {headline, summary}. Restores the original h2 + summary paragraph structure the design had before the correction -- the un-reverted .working/ mockups already showed exactly this. Decisions taken with the user: (1) FR-13's two-source corroboration rule is EXTENDED to cover headlines, since its wording scoped it to 'a Summary' and a short headline rendered beside the outlet name is the more exposed field; (2) the ~260-char one-breath budget covers the Summary alone, with the headline (~70 chars) added on top; (3) headline uses the already-existing, until-now-unused headline-md token (24px, below the sentence's 28px mobile size, so the dominance rule holds) and the Summary moves to the grotesque, as DESIGN.md's Typography section already prescribed. Updated Brand & Style / Typography / Components / Do's and Don'ts in DESIGN.md and Information Architecture / Voice and Tone / State Patterns / Interaction Primitives / Accessibility Floor in EXPERIENCE.md. Accessibility Floor gained a heading-hierarchy rule it never had: the page's first real outline (one h1, one h2 per item).
